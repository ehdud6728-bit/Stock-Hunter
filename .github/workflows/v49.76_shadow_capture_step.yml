# Add this step immediately BEFORE:
#   - name: Show v4.4.9.53.8.49.76.6.7.3 files
#
# It patches only the ephemeral GitHub Actions checkout.
# The scanner's existing production return/output is unchanged.

      - name: Install R1.7.6 copy-only LIVE SHADOW source capture
        shell: bash
        run: |
          set -euo pipefail
          python apply_closebet_live_shadow_capture_r176.py Closing_bet_scanner_v2.py
